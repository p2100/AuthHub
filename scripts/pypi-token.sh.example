#!/bin/bash
# PyPI Token 配置示例
# 
# 使用方法:
# 1. 复制此文件: cp pypi-token.sh.example pypi-token.sh
# 2. 编辑 pypi-token.sh，填入你的真实 PyPI token
# 3. 加载环境变量: source scripts/pypi-token.sh
# 4. 或者在 ~/.bashrc 或 ~/.zshrc 中添加 export UV_PUBLISH_TOKEN='your-token'
#
# 注意: pypi-token.sh 已被 .gitignore 忽略，不会提交到 git

export UV_PUBLISH_TOKEN='your-pypi-token-here'

